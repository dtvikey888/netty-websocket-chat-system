server:
  port: 8088

# Netty WebSocket 配置（纯内存版）
netty:
  websocket:
    port: 8081 # WebSocket 服务独立端口，与 HTTP 端口区分

# 日志配置
logging:
  level:
    com.yqrb: INFO
    io.netty: WARN
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{50} - %msg%n"

#swagger.enable: true
spring:
  cache:
    type:
      redis
  datasource:                                   # 数据源的相关配置
    url: jdbc:mariadb://127.0.0.1:3306/yqrbapp253?allowMultiQueries=true&useUnicode=true&characterEncoding=UTF-8&autoReconnect&useSSL=false
    password: fjw986321
  redis:
    database: 0
    host: 127.0.0.1
    port: 6379
    password: fjw3989099
    jedis:
      pool:
        max-active: 8
        max-wait: -1ms
        max-idle: 500
        min-idle: 0
        testOnBorrow: true
        testOnReturn: true
        testWhileIdle: true
    lettuce:
      shutdown-timeout: 0ms
mybatis:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yqrb.mapper.CustomerServiceMapperCustom">

    <!-- 结果集映射 -->
    <resultMap id="CustomerServiceResultMap" type="com.yqrb.pojo.vo.CustomerServiceVO">
        <id column="id" property="id"/>
        <result column="service_staff_id" property="serviceStaffId"/>
        <result column="service_name" property="serviceName"/>
        <result column="service_phone" property="servicePhone"/>
        <result column="status" property="status"/>
        <result column="login_time" property="loginTime"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <!-- 新增客服（管理员接口，接收VO参数） -->
    <insert id="insertCustomerService" parameterType="com.yqrb.pojo.vo.CustomerServiceVO">
        INSERT INTO customer_service (
        service_staff_id,
        service_name,
        service_phone,
        status,
        create_time,
        update_time
        ) VALUES (
        #{serviceStaffId},
        #{serviceName},
        #{servicePhone,jdbcType=VARCHAR},
        'ONLINE', <!-- 默认状态：在线 -->
        CURRENT_TIMESTAMP, <!-- 自动填充创建时间 -->
        CURRENT_TIMESTAMP  <!-- 自动填充更新时间 -->
        )
    </insert>

    <!-- 更新客服状态和最后登录时间 -->
    <update id="updateCustomerStatus">
        UPDATE customer_service
        SET status = #{status},
            login_time = #{loginTime},
            update_time = #{updateTime}
        WHERE service_staff_id = #{serviceStaffId}
    </update>

</mapper>